import { characters } from "@/data/characters";
export function CharacterGallery() { return <section id="герои" className="section characters page-width"><div className="section-heading"><p className="eyebrow ink">02 / Главные роли</p><h2>Герои в центре кадра.</h2><p>Карточки медленно оживают при просмотре.</p></div><div className="character-grid">{characters.map((character, index)=><article className="character-card" style={{ animationDelay: `${index * .25}s` }} key={character.name}><img src={character.image} alt=""/><div><p className="eyebrow ink">{character.drama}</p><h3>{character.name}</h3><p>{character.personality}</p><small>За что их любят</small><b>{character.reason}</b></div></article>)}</div></section>; }